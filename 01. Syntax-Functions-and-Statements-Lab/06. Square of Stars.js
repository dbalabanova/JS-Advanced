function solve(x) {
    if(typeof(x)==='undefined'){
        console.log(typeof(x));
    } else {
        for (let i = 0; i < x; i++) {
        console.log('*'.repeat(x).split('').join(' '));
            
            
        }
        
    }

}
solve(2)